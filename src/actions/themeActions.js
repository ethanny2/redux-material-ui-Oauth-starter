import { TOGGLE_THEME } from '../types';

/* Thats it! The defined light and dark mode objects/themes are
pre-defined in the themeReducer file*/
export const toggleTheme = () => ({ type: TOGGLE_THEME });
